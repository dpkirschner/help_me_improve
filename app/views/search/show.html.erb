<pre>
	<% @matches.each do |match| %>
	   	<%= match["participants"][0]["timeline"]["role"]%>
	   	<%= match["participants"][0]["timeline"]["lane"]%>
	   	<%= match["participants"][0]["championId"]%>
   		</br>
	<% end %>
</pre>